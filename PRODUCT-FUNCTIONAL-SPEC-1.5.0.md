# Pupu Desktop 1.5.0 产品功能说明

## 1. 版本目标

1.5.0 是“动作连贯性、日常自主行为、相册记忆与模型兼容”专项升级。版本继续沿用现有 Pupu Desktop 的 WPF 桌面宠物、PersonalityBehaviorV2、外部图集清单和本地记忆架构，并重点完成：

- 锁定朴朴的拿破仑矮脚猫身份，修正新增素材中腿部过长的问题；
- 增加侧前、侧后等斜向移动过渡和更稳定的体型、落脚点；
- 新增自主如厕、低频舔毛、蓝色长方垫小窝等日常行为；
- 升级四种魔法的光效、飞行轨迹和动作收尾；
- 增加本地相册、子相册、宠物档案画像、长期性格摘要与好感阶段展示；
- 扩展 OpenAI、Qwen、DeepSeek、自定义服务商和 Chat Completions / Responses 两种请求格式；
- 将短期对话、长期行为记忆和经主人明确授权的相册视觉检索接入同一角色上下文。

## 2. 不可污染的建模身份

朴朴始终是银灰黑白长毛拿破仑矮脚幼猫。V11 新增和替换素材必须同时满足：

- 宽圆幼态脸、短口鼻、黄绿色圆眼；
- 额头白色中轴和粉黑拼接鼻头中央的小黑斑；
- 身体是自然偏长的幼猫躯干，不能为了 Q 版效果压成短圆球；
- 四肢明显短于普通猫，站立、走路、埋砂和牵引绳动作都不能画成长腿；
- 保留完整、蓬松的大尾巴和银灰黑白长毛花纹；
- 日常表情自然可爱，困倦才半闭眼或张嘴哈欠，生气以回避视线、转身和离开为主；
- 魔法服装、背带、牵引绳、小窝只改变当前动作状态，不改变脸型、毛色、腿长、体型或尾巴。

V11 不重画无关的 V9 Core、Directions、Touch、WalkModes 和 V8 Activity 图集。已验收的常态身份素材继续作为视觉基线；新增视觉以独立图集进入运行清单。

## 3. V11 动作与装备素材

### 3.1 三张新增或替换图集

| 图集 ID | 文件 | 网格（行×列） | 内容 |
| --- | --- | ---: | --- |
| `lifeEquipment` | `pupu-life-equipment-youthful-v11.png` | 4×8 | 自主如厕与埋砂、低频舔毛、蓝色长方形垫子及窝内睡眠、孔雀蓝牵引绳互动 |
| `motion` | `pupu-motion-youthful-v11.png` | 11×8 | 背带/无背带四种斜向步态、扫帚八方向飞行、矮脚侧躺微动、窗口上沿矮脚趴卧 |
| `specials` | `pupu-specials-youthful-v11.png` | 5×8 | 低趴八方向视线、四种增强魔法 |

三张图集都使用 256×256 RGBA 单元格、8 列和至少 20px 四边透明安全区。同一动作行共用缩放率和落地点，禁止逐帧拉满导致体型忽大忽小。

### 3.2 斜向移动与侧躺

- 背带与无背带模式都增加左前、右前、左后、右后四组斜向步态；
- 运行时按真实位移向量选择八方向素材，不再只用左右前后四个方向硬切；
- 换方向时尽量保留当前步态相位，减少腿部突然换脚；
- 每次溜达都必须伴随桌面窗口的真实坐标位移，禁止原地踏步；
- 新增矮脚侧躺微动，用于安静呼吸、尾尖轻动等低干扰驻留。

### 3.3 日常舔毛

- `self.groom` 使用舔胸毛、舔侧腹和回身梳理等变体；
- 这是安静、原地、低频的自主行为；
- 单次最短驻留 75 秒，完成后至少冷却 10 分钟；
- 压力较高时降低触发倾向，避免频繁重复舔毛。

### 3.4 蓝色长方形垫子

- `rest.bed` 使用预先绘制的蓝色长方形垫子与窝内睡眠造型；
- 小窝是完整预设素材，不是运行时叠加一块矩形；
- 低唤醒、高疲劳和高安全感时更容易选择；
- 单次原地驻留至少 7 分钟，完成后至少冷却 15 分钟。

### 3.5 孔雀蓝牵引绳

- 牵引绳、背带和猫本体共同绘制为完整预设素材；
- 新增斜向行走保持孔雀蓝颜色、背带轮廓和矮脚比例稳定；
- 背带遛猫与无背带自由探索都能使用一级“停下”安全中断。

## 4. 自主如厕

如厕从主人手动照料改为宠物自主日程：

- 每个本地自然日随机创建 2–3 个时隙；
- 当日计划持久化，同一时隙只能预留和完成一次；
- 只有 `toilet_due` 信号到期时才进入统一自主行为候选；
- 启动或恢复时错过的时隙直接跳过，不追补、不责怪主人，也不形成照料欠账；
- 主菜单不再保留手动铲屎入口。

完整动作链为：

1. 进入猫砂盆；
2. 自然如厕；
3. 以较低概率抬头观察，抬头不是强制姿势；
4. 固定衔接“抓抓开花、狠狠埋砂”；
5. 离开猫砂盆并恢复普通低趴。

如厕已经发生但动作被中断时，也会尽量执行短版埋砂收尾，避免从排泄帧直接跳回待机。

## 5. 真实桌面与窗口环境

- Windows 运行时通过系统 API 枚举真实窗口句柄并读取窗口矩形、前台状态、进程类别、监视器工作区和 DPI；
- 浏览器窗口优先，但符合条件的普通应用窗口也可成为目标；
- 朴朴可在真实窗口上沿趴卧、沿边移动，并随目标窗口位置变化；
- 目标窗口失效、最小化或不再适合栖息时，立即解除跟随并回到普通待机；
- 切换到普通桌面移动或魔法移动时清除旧窗口句柄，避免被过期窗口坐标拉回；
- 只读取公开的窗口几何和进程类别，不读取网页 DOM、窗口正文、键盘输入或屏幕像素。

如果系统限制导致无法取得合适窗口，运行时退回当前显示器工作区边缘，不伪造或保存固定的桌面路线。

## 6. 魔法视觉与动作闭环

### 6.1 统一视觉

- 四种魔法的斗篷统一为紫色魔法风格；
- 飞行和施法加入流光、粒子拖尾及更大的弧光；
- 所有魔法服装仍保持朴朴的矮脚、圆脸、毛色和体型。

### 6.2 Accio Broom

- 朴朴念出 “Accio Broom”，披上紫色斗篷并跨上扫帚；
- 桌面飞行持续 1 分钟；
- 每段路线根据当前位置与随机目标计算真实坐标；
- 支持左、右、上、下及四个斜向，共八方向飞行素材；
- 采用高速曲线、弧线偏移和轻微上下浮动，避免单一路线重复；
- 飞行始终约束在当前显示器工作区，可用一级“停下”中断。

### 6.3 Apparate

- 紫色斗篷转圈时扩大消失弧光；
- 窗口平滑淡出、隐藏约数秒，然后出现在当前显示器另一随机位置；
- 重新出现时先保持魔法装扮约 1.5 秒，再平滑恢复普通造型；
- 不跳回主显示器，也不会复用原位置假装移形。

### 6.4 Petrificus Totalus

- 朴朴逐渐僵硬、石化，再变成印有宠物头像的银币；
- 银币增加金属高光和闪烁，参考纪念币式清楚浮雕质感，但保持银币设定；
- 高光节奏参考 Dogecoin 金币的饱满浮雕和亮边质感，但不复制其标志或金色材质；
- 银币原地保持，直到主人解除或选择“停下”；
- 解除后必须完整播放伸懒腰放松，再恢复普通低趴，形成情绪闭环。

### 6.5 Scourgify

- 魔杖光环显著放大，增加沿边擦拭的流光；
- 优先沿真实目标窗口四边移动擦亮；没有合适窗口时沿当前显示器工作区边缘执行；
- 出于桌面安全边界，不隐藏、缩小、重排或修改真实桌面图标；
- 窗口边缘擦亮是稳定的安全实现，不会把视觉效果冒充为真实文件操作。

四种魔法仍是统一自主行为评分中的正式候选，并受压力、疲劳、安全感、好奇、淘气、勿扰和会议状态影响。每个本地自然日最多自发施法一次；主人手动请求不占用该自发额度。

## 7. 控制面板与素材库

### 7.1 高级调试

高级调试页不再用连续纯文本堆叠。运行状态、长期关系、行为规则和候选评分都分别放入独立圆角卡片，并使用可换行矩阵布局。

### 7.2 动作素材库

- 普通动作继续显示分类、真实缩略图、动作说明和桌面预览；
- 四种魔法位于独立“魔法特辑”页签；
- 节日限定位于独立“节日特辑”页签；
- “节日特辑”只显示文字标签和日期门槛，不显示缩略图，也不提供预览或绕过日期的入口；
- 圣诞帽只在 12 月 25 日、万圣节斗篷帽只在 10 月 31 日、春节红围巾只在农历正月初一出现；
- 主人生日祝福只在档案生日当天触发，有出生年份时结合当年周岁。

## 8. 宠物档案

宠物档案保留可编辑的中文名、英文名、品种、性别、宠物生日、对主人昵称、和主人关系及主人生日，并新增：

- 顶部专属画像；
- 基于已保存五维性格和跨天行为偏好的长期性格摘要；
- 当前好感阶段展示；
- “观察中、熟悉、亲近、家人”等可编辑展示阶段。

手动选择阶段只改变主人看到的展示文字，底层信任、主动接受和触摸接受仍按真实互动继续演进。档案保存后仍会同步到本地自称、主人称呼、长期记忆和模型 System Prompt。

## 9. 本地宠物相册

### 9.1 文件夹链接

- 主人选择一个真实本地图片根目录；
- 应用保存目录和索引，不复制、移动、修改或删除原图；
- 支持 JPEG、PNG、WebP、BMP 和 GIF 的本地索引；
- 目录暂时失联时保留子相册名称、主题和时间信息。

### 9.2 子相册

每个子相册可保存：

- 名称；
- 根目录内的相对路径；
- 主题；
- 开始日期和结束日期；
- 成长阶段。

相对路径不能逃出主人选择的根目录。子相册索引可以删除，但不会删除本地文件夹或照片。

### 9.3 检索

可按关键词、主题、成长阶段、文件名、相对路径和日期检索。相册卡显示封面、可用状态和照片数量；检索结果用于主人浏览，也可在模型对话中提供相关记忆。

## 10. 模型 API 与记忆

### 10.1 服务商与格式

- 内置服务商选择：OpenAI、Qwen、DeepSeek、Custom；
- 请求格式：OpenAI Chat Completions 和 OpenAI Responses；
- 服务商预设负责默认地址、格式与能力提示；
- 不支持某种格式或视觉能力的预设会在发送前拒绝错误组合；
- 远程地址必须使用 HTTPS，本机服务可使用 localhost；
- API 密钥按“服务商 + 地址”隔离存入 Windows 凭据管理器，不进入记忆、日志或设置 JSON；
- 429 和服务端 5xx 使用有界重试，过长的 `Retry-After` 不阻塞前台会话。

### 10.2 System Prompt

每次模型对话都注入：

- 完整宠物档案与对主人称呼；
- 五维天生性格；
- 当前状态和关系；
- 与本轮问题相关的有限长期记忆；
- 朴朴的自主性、语言风格和技术信息隔离规则。

模型输出仍需经过角色边界检查。API、提示词、路径、评分、日志等技术语言不能进入宠物气泡。

### 10.3 长短期记忆

- 长期记忆继续由 `pupu-memory.md`、`events.md`、确认事实、情景记忆和跨天偏好组成；
- 短期对话保存在 `conversation.json`；
- 主人可在 8–12 轮之间选择短期会话窗口，默认 10 轮；
- 会话采用原子写入并自动裁剪，不无限增长；
- 普通对话、模型失败回退和重新启动后都能保持必要的上下文连贯性。

### 10.4 相册联动与显式授权

当主人在对话中提到照片、相册、成长阶段、节日、生日或往事时，应用可先按关键词、主题和日期检索相关相册：

- 默认只把相册名、主题、成长阶段和日期作为文字记忆；
- 只有“启用视觉输入”和“允许发送本轮检索到的相册图片”两个开关同时开启时，才读取并发送图片；
- 每轮最多发送 2 张 JPEG、PNG 或 WebP，单图不超过 6 MB；
- 图片会编码为请求内数据，不发送本地绝对路径；
- 不支持视觉的服务商预设会阻止图片发送；
- 关闭任一开关后，相册仍可作为本地文字记忆使用，但不会向模型发送照片内容。

## 11. 正式素材范围

正式清单共 11 张图集、576 个 256×256 RGBA 单元格：

| 图集 | 网格（行×列） | 单元格 |
| --- | ---: | ---: |
| Core V9 | 6×8 | 48 |
| Life V6 | 8×8 | 64 |
| Directions V9 | 4×8 | 32 |
| Touch V9 | 6×8 | 48 |
| Routines V6 | 8×8 | 64 |
| WalkModes V9 | 8×8 | 64 |
| Activity V8 | 8×8 | 64 |
| LifeEquipment V11 | 4×8 | 32 |
| Motion V11 | 11×8 | 88 |
| Specials V11 | 5×8 | 40 |
| Seasonal V10 | 4×8 | 32 |

## 12. 验收要求

- Pupu.Behavior、Pupu.Tests 和 WPF Desktop Release 编译通过；
- 自动测试 `38/38` 通过；
- XAML 只读/可编辑绑定检查通过；
- 11 张图集、576 格全部非空、可完整解码并满足 20px 透明安全区；
- 新增斜向移动行满足项目体型漂移与相邻中心位移门槛；
- 576 格最小有效主体 `69×100px`、最低清晰度 `111.3`、最大移动体型漂移 `1.236×`、最大相邻中心位移 `9.01px`；
- Windows x64 自包含包可完整解压，且只包含清单列出的正式运行图集；
- 应用包、源码包和工作区 V11 正式文件哈希一致；
- Windows 10/11 实机重点检查混合 DPI、扩展屏窗口跟随、八方向步态、扫帚一分钟飞行、Apparate 重现和相册文件夹失联恢复。
