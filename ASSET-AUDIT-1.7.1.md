# Pupu Desktop 1.7.1 银币素材审计

## 更新范围

- `normalColor` 直接采用主人确认的侧脸魔法银币母版，完整保留环形文字、月亮、星星、魔法纹路、侧脸角度和直立耳朵。
- `normalFaded` 从同一母版确定性去色，构图和边界不变。
- `unhappyColor` 仅改变双眼：整体轻微下垂，纵向开合约缩小 12–15%；不改变耳朵、头部角度和环形装饰。
- `unhappyFaded` 从对应不开心彩色版确定性去色。
- `back` 使用同银质、同环形文字和同魔法纹路的浮雕猫爪。

## 状态与运行时

五态继续登记在 `Pupu.Desktop/Assets/pupu-assets.json`：

- `normalColor`
- `normalFaded`
- `unhappyColor`
- `unhappyFaded`
- `back`

五帧均位于 `gazeCoin:2`，保持 256×256 RGBA、同一圆心、196px 外径和至少 20px 透明安全区。石化后拖拽只移动窗口，单击恢复彩色，双击翻面。

## 验证结果

- 素材清单 JSON：通过。
- 五态键、行和帧范围：通过。
- 银币行 8 格完整解码、非空和透明安全区：通过。
- 全量像素审计：616 个图集单元格和 48 个独立动作帧通过。
- 有效主体最小值：`68×101px`。
- 最低清晰度：`228.0`。
- 最大移动体型漂移：`1.238×`。
- 最大相邻中心位移：`8.54px`。

## 构建验证

- `Pupu.Desktop` Release / `win-x64`：编译通过，`0 warning / 0 error`。
- `Pupu.Tests`：`60/60` 通过。
- 自包含单文件发布：通过。
- `Pupu.exe`：验证为 `PE32+`、`x86-64`、`Windows GUI` 子系统。
- 发布目录：清单引用的 16 个 PNG 全部存在，未引用 PNG 为 0。
- 发布 ZIP：完整解压测试通过。

当前构建容器不是 Windows，因此没有在本机启动 WPF 窗口；首次启动冒烟测试仍应在 Windows 10/11 完成。
