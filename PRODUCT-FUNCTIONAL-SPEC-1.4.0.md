# Pupu Desktop 1.4.0 产品功能说明

## 1. 产品目标

1.4.0 在不替换 V9/V8 当前建模形象的前提下，补充“原地看鼠标、宠物档案、状态参与、宠物魔法、节日装扮和生日祝福”。新增视觉全部进入独立 V10 图集；原有核心动作、触摸、行走、睡眠和板鸭趴继续使用已验收素材。

## 2. 原地鼠标视线跟随

- 生效条件：宠物已完成启动、正在原地安静行为、没有长互动、没有桌面移动、没有触摸逃离、没有石化，控制面板也不处于前台操作状态。
- 感知范围：鼠标距宠物画面中心约 300 个逻辑像素内。
- 方向：中立、左、左上、上、右上、右、右下、左下，共八帧。
- 行为：只替换眼睛和头部朝向素材，不移动宠物窗口，不启动新的主人互动，也不修改关系。
- 退出：鼠标离开范围、面板获得焦点或其他动作开始时，恢复进入跟随前的行为和动画。

视线跟随使用操作系统全局鼠标坐标，因此鼠标不必已经进入宠物图片；它不会读取点击内容、网页内容或键盘输入。

## 3. 宠物档案

控制面板新增“宠物档案”页，可编辑：

- 中文名，默认“朴朴”；
- 英文名，默认“Pupu”；
- 品种，默认“银灰黑白长毛曼基康”；
- 性别，默认“公猫”；
- 宠物生日，可空；
- 对主人昵称，默认无；为空时称呼“主人”；
- 和主人关系，默认“弟弟”；
- 主人生日，可空。

点击“保存宠物档案”后：

- 更新本地 `profile.json`；
- 写入主人确认事实；
- 更新可编辑 Markdown 记忆；
- 更新面板标题和档案摘要；
- 本地宠物气泡使用新的中文名和主人称呼；
- 模型对话系统身份包含完整档案；
- 若保存的主人生日就是当天，立即进入当日生日祝福检查。

主人明确填写的档案优先于自动推断。姓名、品种、关系和日期会进行长度与日期范围校验。

## 4. 状态性参与与拒绝

主人从右键菜单发起下列行为时，宠物不再保证每次参加：

- 猫粮、冻干、罐头；
- 背带遛猫、自由探索；
- 清理猫砂；
- 梳毛；
- 逗猫棒、激光笔；
- 趴下、打滚、追尾；
- 四种宠物魔法。

参与概率综合：

- 饱腹、精力、干净、猫砂负担；
- 压力、疲劳、安全感、玩耍意愿、好奇心；
- 活泼、独立、敏感、淘气等天生性格；
- 信任和触摸接受；
- 对具体行为与场景形成的偏好。

拒绝时宠物会用角色语言说明，例如太饱、太累、想安静、现在不想追或不想梳毛。拒绝记录为中性互动，不扣信任，不形成照料欠账，也不把技术概率显示在气泡中。

## 5. 宠物魔法

桌面右键菜单新增一级“宠物魔法”，二级名称固定为英文。

### 5.1 Accio Broom

- 中文含义：飞来咒·扫帚版。
- 动作：念出“Accio Broom”，穿斗篷、召来扫帚并跨上。
- 桌面效果：只在宠物当前显示器工作区内生成随机飞行路线，持续 1 分钟。
- 可随时使用一级“停下”结束。

### 5.2 Apparate

- 中文含义：幻影移形。
- 动作：穿斗篷原地转圈并闪光。
- 桌面效果：宠物窗口短暂透明消失，约几秒后在当前显示器另一个随机位置出现。
- 不跳回主屏，也不跨越到其他显示器。

### 5.3 Petrificus Totalus

- 中文含义：统统石化。
- 动作：逐渐全身僵硬、变成石像，再变成带宠物头像的银币。
- 保持：银币原地循环，不接受普通动作。
- 解除：右键“宠物魔法 → 解除石化”或一级“停下”。

### 5.4 Scourgify

- 中文含义：清理一新。
- 动作：挥动魔杖并产生闪光。
- 桌面效果：优先沿当前可栖息窗口的上边缘移动擦亮；没有合适窗口时沿当前屏幕上缘移动。
- 安全边界：不修改、隐藏、缩放或重排真实桌面图标。

## 6. 自发魔法

- 宠物在自主决策时有较高概率尝试施法。
- 仍受压力、疲劳、好奇、淘气、活泼、勿扰和会议状态影响。
- 每个本地自然日最多一次自发魔法。
- 自发额度在动作开始前保存；即使施法中断或应用异常退出，同一天也不会再次自发触发。
- 主人手动请求魔法不占用“每日一次自发魔法”额度。

## 7. 节日装扮与生日

- 圣诞帽：仅公历 12 月 25 日；
- 万圣节斗篷帽：仅公历 10 月 31 日；
- 春节红围巾：仅农历正月初一；
- 主人生日：仅档案中主人生日的月日匹配当天。

每种节日/生日效果同一天只自动出现一次。主人生日有出生年份时，祝福会使用当年周岁；只填写月日或年龄不合法时使用不含年龄的祝福。

节日限定在动作素材库的独立“节日特辑”页中只以文字标签展示，不提供缩略图或预览；右键菜单也不提供绕过日期的常规入口。

## 8. 控制面板

- 新增“宠物档案”一级页。
- “动作素材库”内部增加“全部动作”和“魔法特辑”两个页签。
- “魔法特辑”展示四种魔法的真实 V10 缩略图和原地素材预览。
- “节日特辑”使用纯文字卡显示“圣诞限定”“春节限定”“主人生日限定”等标签，永不显示素材预览。
- 面板标题随保存后的中文名和英文名更新。
- 宠物角色气泡与系统/连接/验证信息继续分流。

## 9. 素材范围

正式素材为九张图集、456 个 256×256 RGBA 单元格：

| 图集 | 网格 | 用途 |
| --- | ---: | --- |
| Core V9 | 8×6 | 核心表情与玩具 |
| Life V6 | 8×8 | 生活互动 |
| Directions V9 | 8×4 | 四向移动 |
| Touch V9 | 8×6 | 触摸与关系 |
| Routines V6 | 8×8 | 待机、进食与背影 |
| WalkModes V9 | 8×8 | 背带及自由行走 |
| Activity V8 | 8×8 | 激光、睡姿、板鸭趴、窗口趴卧 |
| Specials V10 | 8×5 | 视线与四种魔法 |
| Seasonal V10 | 8×4 | 三种节日装扮与生日 |

V10 图集只新增视觉，不覆盖 V9/V8 文件。所有正式格继续满足完整主体、透明背景和 20px 安全边距。

## 10. 验收要求

- WPF Release 编译 0 错误；
- 行为测试 34/34；
- XAML 只读/可编辑绑定检查通过；
- 456 格非空、完整 PNG 解码和 20px 透明边距检查通过；
- 有效主体不低于项目尺寸与清晰度门槛；
- 原有移动图集体型漂移不超过 1.25 倍，相邻帧主体中心位移不超过 10px；
- Windows x64 自包含包可完整解压，包含九张清单图集；
- 应用包和源码包内 V10 文件哈希与工作区正式素材一致。
