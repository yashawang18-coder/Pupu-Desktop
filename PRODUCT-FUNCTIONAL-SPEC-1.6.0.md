# Pupu Desktop 1.6.0 产品功能说明

## 1. 版本目标

1.6.0 是“V12 动作统一、连续真实移动、双入口对话、相册描述记忆与模型测试修复”专项升级。版本在 1.5.0 的拿破仑矮脚身份、每日如厕、窗口环境、魔法和相册基础上完成：

- 重制用户指定的旧动作，统一为清晰可爱的同一只朴朴；
- 让遛猫、自主溜达和扫帚飞行产生连续、随机且可见的真实坐标位移；
- 让主人发起的魔法必定执行，同时保留普通互动的心情拒绝；
- 增加双面猫爪银币、贴地摆尾视线、高信任走近轻微放大；
- 将动作库、相册、产品说明和技术说明整理为更易浏览的卡片和页签；
- 修复 DeepSeek 在面板测试失败的问题，并把 Markdown 宠物系统提示词接入高级模型对话；
- 让真实图片文件夹、子目录元数据和逐图描述进入相册检索与回忆。

## 2. 形象与素材原则

朴朴始终是同一只银灰黑白长毛拿破仑矮脚幼猫：

- 宽圆幼态脸、短口鼻、黄绿色圆眼；
- 额头白色中轴、粉黑拼接鼻头中央小黑斑；
- 自然偏长的躯干，不能压成短圆球；
- 明显短腿，站立、进食、埋砂、转圈和行走都不能变成长腿猫；
- 完整蓬松的大尾巴，不能因切片或动作幅度被裁掉；
- 毛色、脸型、腿长和体型不因魔法、梳子、猫砂盆、背带或小窝而改变。

V12 以主人本轮上传的四张高清实拍固定身份，以已验收的“眨眼哈欠”“鼠标视线跟随”“向下跑”固定清晰度、可爱度、头身比与落点。每格仍为 256×256 RGBA、8 列、四边至少 20px 透明安全区；同一动作行共用缩放率和落地点。

## 3. V12 重制动作

### 3.1 安静与自我梳理

- **低趴观察**：胸口贴地，保持圆脸、矮脚和完整尾巴；
- **舔脚吃脚**：抱住后脚舔毛并轻轻啃爪，作为独立低频自主行为；
- **日常舔毛梳理**：舔前爪、胸口、侧腹和大尾巴，单次至少 75 秒、冷却至少 10 分钟；
- **小猫侧躺／侧躺慢呼吸**：使用矮脚侧躺微动，安静时慢呼吸和轻摆尾尖；
- **侧躺打滚**：露肚、翻身、回到侧躺，完整展示动作上下文；
- **追尾转圈**：追着大尾巴快速转圈，短腿与躯干比例稳定。

### 3.2 进食与照料

- 一级菜单“吃饭”改名为“吃一下”；
- **冻干饿猫扑食**和**罐头饿猫扑食**使用新名称；
- 两种急切进食都只播放一次扑近起步，随后循环后半段进食帧，避免反复离碗又扑向碗；
- **梳理毛发**使用孔雀蓝梳子完整预设素材，不出现手或人体局部；
- **过度 rua**只表现猫本体，以移开视线、贴地甩尾和退开表达边界，不哈气、不露齿、不攻击。

### 3.3 自主、环境与移动

- **偷偷捣乱**：拨弄笔筒后装作无事发生；
- **自由外出左前／右前／左后／右后**与正面、背面、左右保持同一矮脚体型；
- 自由外出背影按同组步态重新归一化，避免切换方向时突然变大；
- 正面、背面、左右和斜向切换时保留当前步态相位。

### 3.4 信任亲近

高信任触摸选择“信任亲近”时：

1. 播放走近、慢眨眼、蹭头和安心趴下；
2. 形象随走近从 100% 平滑放大到约 106%；
3. 不移动透明窗口；
4. 动作或触摸会话结束后恢复 100%。

## 4. 自主如厕闭环

如厕继续按每个本地自然日 2–3 个随机时隙自主触发，错过的离线时隙不追补。V12 把动作拆为四行：

1. **检查猫砂并进入**；
2. **自发如厕低头微动**；
3. **如厕概率抬头**；
4. **爪爪开花埋屎并离开**。

抬头只是概率变体，不会每次强制出现。如厕完成后必须衔接“爪爪开花埋屎”；排泄已经提交但动作中断时，也尽量执行短版埋砂收尾。

## 5. 鼠标视线与贴地摆尾

当朴朴原地安静、附近有鼠标且没有长动作、移动、睡眠、石化或前台面板操作时：

- 眼睛和头部跟随鼠标的八个方向；
- 身体保持低趴，不因视线方向变成端坐；
- 使用两个落点一致的尾巴相位；
- 即使鼠标方向不变，也约每 420ms 切换尾巴相位，形成持续贴地轻摆；
- 鼠标离开范围后恢复进入视线跟随前的原动作。

## 6. 连续真实移动

### 6.1 遛猫与自主溜达

所有路线都在运行时随机生成，不保存固定航点：

- 正式遛猫混合屏幕大范围目标和附近目标；
- 自主溜达范围可大可小，但每段距离必须非零；
- 路段使用平滑曲线、随机弯曲和少量小跳；
- 每约 16ms 更新透明窗口的 `Left` / `Top`；
- 所有坐标限制在宠物当前监视器工作区；
- 一段完成后立即规划下一段，避免停在原地播放步态。

孔雀蓝背带与无背带模式都支持一级“停下”，并按已发生进度记录 Interrupted。

### 6.2 扫帚飞行

`Accio Broom` 的一分钟飞行使用每轮重新打乱的八方向桶：

- 一轮覆盖左、右、上、下、左上、右上、左下、右下；
- 每段采用约 1450–2300 DIP/s 的高速、大跨度弧线；
- 增加轻微 flutter，但不以原地上下抖动替代飞行；
- 段间连续衔接，不加入普通步行式长停顿；
- 从工作区边缘出发时先快速回到可继续覆盖全方向的位置；
- 轨迹始终在当前显示器工作区内。

## 7. 魔法参与与完整反馈

### 7.1 参与规则

- 主人从右键菜单明确发起四种魔法时，朴朴必须执行，不经过普通互动的心情参与概率；
- 普通投喂、遛猫、梳毛、玩具和姿势仍可根据饱腹、疲劳、压力、关系和具体偏好礼貌拒绝；
- 自发魔法仍作为正式行为候选进入统一评分，每个本地自然日最多一次。

### 7.2 双面石化银币

`Petrificus Totalus` 结束后变为带金属高光的银币：

- 正面为宠物头像；
- 背面为猫爪；
- 点击银币以横向压缩和专用过渡帧完成翻转；
- 翻转点击不进入普通触摸手势，也不累计 rua 压力；
- 解除石化后仍先播放完整伸懒腰，再恢复低趴。

`Apparate` 继续在重现后保留约 1.65 秒魔法装扮；`Scourgify` 继续沿真实目标窗口或当前屏幕边缘移动擦亮，不修改桌面图标。

## 8. 桌面与面板双入口对话

模型或本地角色对话有两个入口：

- 控制面板“和朴朴聊天”；
- 透明宠物窗下方的轻量输入框。

两处共享同一个 `ChatInput` 和 `SendChatCommand`，因此输入、发送状态、短期会话和模型配置完全一致。按 Enter 或点击“发送”提交；回复统一显示在宠物形象上方气泡。技术错误只进入系统状态，不进入宠物气泡。

## 9. 面板信息架构

### 9.1 右键命名

- 一级“吃饭”改名为“吃一下”；
- “面板”子菜单第一项改名为“打开面板”。

### 9.2 动作素材库

- 普通动作按“安静待机、生活日常、触摸反应、投喂互动、移动”等类别分组为不同页签；
- 魔法使用独立“魔法特辑”；
- 节日使用独立“节日特辑”，只显示文字标签和日期门槛，不提供预览；
- 动作卡继续显示真实缩略图、名称、说明、帧数和快捷预览。

### 9.3 说明页

面板增加：

- **产品设计说明**：产品定位、形象一致性、自主与互动、真实移动、记忆、相册、素材和隐私；
- **代码实现说明**：界面、行为、素材、模型、系统提示词、相册索引、持久化和验证边界。

两页均使用圆角矩阵卡片。宠物档案、相册、动作库等产品页排在前部；模型、Markdown 记忆、代码说明和高级调试排在后部。

## 10. 真实文件夹相册

### 10.1 根目录与子相册

- 主人链接一个真实本地图片根目录；
- 应用不复制、移动、改名或删除原照片；
- 根目录下的一级子文件夹会自动发现并生成稳定 ID；
- 文件夹名称可解析 `YYYY-MM-DD`、`YYYY-MM`、中文年月日等时间，以及移除时间后的主题；
- 手动子相册仍可设置名称、相对路径、主题、开始／结束日期和成长阶段；
- 子目录卡以矩阵方式展示封面、时间、主题、数量和可用状态。

### 10.2 逐图预览与描述

进入子相册后：

- 可逐张查看缩略图和大图预览；
- 显示文件名、时间、主题和成长阶段；
- 可编辑并保存每张图片的描述；
- 描述保存在 `albums.json` 的相对路径索引中，不写回图片文件；
- 清空描述会删除对应描述索引，不删除照片。

### 10.3 检索与回忆

检索同时匹配文件名、相对路径、相册名、主题、成长阶段和逐图描述。相关结果可进入高级模型对话的文字记忆；只有视觉和相册图片发送两个开关同时开启时，才发送最多 2 张符合限制的图片，且不发送本地绝对路径。

## 11. 模型 API 与 DeepSeek 修复

### 11.1 服务商兼容

继续支持 OpenAI、Qwen、DeepSeek 与 Custom，以及 Chat Completions / Responses 两种协议。服务商预设负责默认地址、模型、协议和视觉能力。

DeepSeek 预设调整为：

- 默认端点 `https://api.deepseek.com/chat/completions`；
- 默认模型 `deepseek-v4-flash`；
- 输入 `https://api.deepseek.com` 或以 `/v1` 结尾的兼容地址时自动补全 `/chat/completions`；
- 不把 DeepSeek 声明为 Responses 或视觉预设。

### 11.2 面板测试连接

“测试连接”与“启用模型对话”解耦：

- 不要求先勾选启用；
- 使用当前面板地址、模型和凭据直接发出最小 Chat 请求；
- 不把测试回复交给宠物语言边界，避免服务连接成功却因回复格式被判失败；
- 仍验证地址、协议、模型名和密钥；
- HTTP 错误只显示经过长度限制和密钥清理的服务端摘要。

## 12. Markdown 宠物系统提示词

`pupu-memory.md` 新增：

```markdown
## 宠物系统提示词
- 主人明确保存的宠物角色补充设定
```

该段落支持保存、重新载入和导出，规范化后存入宠物档案。高级模型对话会把它作为 system 背景注入，同时保留固定身份、安全、自主性和技术信息隔离规则；主人自定义提示词不能绕过这些边界。

## 13. 正式素材范围

| 图集 | 网格 | 单元格 |
| --- | ---: | ---: |
| Core V12 | 6×8 | 48 |
| Life V12 | 8×8 | 64 |
| Directions V12 | 4×8 | 32 |
| Touch V12 | 6×8 | 48 |
| Routines V12 | 8×8 | 64 |
| WalkModes V12 | 8×8 | 64 |
| Activity V8 | 8×8 | 64 |
| LifeEquipment V12 | 3×8 | 24 |
| Motion V12 | 11×8 | 88 |
| GazeCoin V12 | 3×8 | 24 |
| Litter V12 | 4×8 | 32 |
| Specials V11 | 5×8 | 40 |
| Seasonal V10 | 4×8 | 32 |

合计 13 张图集、624 格。

被 V12 替换的旧正式文件已从 `Pupu.Desktop/Assets` 移除，并存入 `AssetSources/legacy-formal-v12`。它们不会进入 1.6.0 运行包。

## 14. 验收结果与发布边界

本轮已完成：

- Pupu.Tests Release `43/43` 通过；
- WPF Desktop Release 编译 `0 warning / 0 error`；
- C# 语法解析通过；
- XAML/XML 解析和显式绑定检查通过；
- 13 张图集、624 格素材审计通过；
- 最小有效主体 `67×100px`；
- 最低清晰度 `111.3`；
- 最大移动体型漂移 `1.238×`，低于 `1.25×` 门槛；
- 最大相邻中心位移 `8.54px`，低于 `10px` 门槛。

发布仍需确保：

- Windows x64 自包含 ZIP 可完整解压；
- `Pupu.exe` 为 Windows x64 GUI；
- 运行包只包含清单登记的 13 张图集；
- V12 在工作区、应用包和源码包中的哈希一致；
- Windows 10/11 实机重点检查混合 DPI、跨屏连续路线、扫帚一分钟飞行、双面银币、桌面对话输入、DeepSeek 凭据和本地相册文件选择。
