# Pupu 1.11.0 一键安装设计

## 用户体验

`Pupu-Setup-x64-1.11.0.exe` 是当前用户安装程序。双击后自动完成安装、
快捷方式创建、系统卸载登记并启动 `Pupu.exe`，不依赖 PowerShell 脚本、
管理员权限或预装 .NET Desktop Runtime。

安装目录固定为：

```text
%LOCALAPPDATA%\Programs\Pupu
```

用户记忆与设置继续位于：

```text
%LOCALAPPDATA%\PupuDesktop
```

覆盖升级和普通卸载都不删除该数据目录。

## 安装安全

1. 安装载荷作为 ZIP 资源内嵌到自包含安装器。
2. 解压时把每个路径规范化，并拒绝逃逸安装暂存目录的条目。
3. 载荷包含逐文件 SHA-256 清单；替换旧版本前验证版本、主程序、素材清单
   和每个正式文件。
4. 新版本先进入同级暂存目录，旧版本改名为备份目录，再原子切换。
5. 快捷方式或卸载登记失败时删除未完成的新版本并恢复旧目录。
6. 安装完成后才启动桌面宠物。

## 卸载

安装程序自身保存为安装目录中的 `Pupu-Setup.exe`，并登记到 Windows
“已安装的应用”。卸载时先停止正在运行的 Pupu，删除桌面和开始菜单
快捷方式、卸载登记及程序目录；共同记忆和设置默认保留。
