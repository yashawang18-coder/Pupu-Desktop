# Pupu 1.11.1 素材完整性与播放连续性修复

## 根因

- PR #1 早期上传的九张 V18 母表和九张 V18 运行 PNG 被统一截断到约 768 KiB；文件头和宽高仍可读取，但 IDAT 数据不完整，Pillow 无法解码。
- Windows 素材门禁此前只读取 `BitmapFrame.PixelWidth/PixelHeight`，未调用 `CopyPixels` 强制解码完整像素，因此截断图没有在打包前失败。
- 冻干和激光追逐把每方向四相直接按 `0-1-2-3` 循环，最后的 `3→0` 形成明显跳变；素材预览器也会把单次动作无限跳回首帧。

## 修复

- 从本地提交 `fdd8355` 的完整工作副本恢复九张 V18 母表与全部 V18 运行图。
- `scripts/repair_runtime_assets.py` 仅替换 V12/V15/V17/V18 外轮廓三像素范围内的绿色溢色；不修改 alpha、裁切、身体比例、落点和内部颜色。
- `scripts/rebuild-v18-assets.py` 重建后自动执行同一去绿处理，并保持重复执行哈希一致。
- 冻干／激光每方向使用 `0-1-2-3-2-1` 六步闭环；日常舔毛使用 `pingPong`。
- 单次动作预览在末帧停止并提供“重新播放”；八方向追逐预览固定展示一个闭合方向。
- Linux/Python 审计检查 PNG 签名与末尾 IEND、完整像素解码、边缘溢绿、透明边距、体型、质心、清晰度、四相脚步和循环闭合。
- Windows/PowerShell 门禁使用 `BitmapFrame.CopyPixels` 强制完整解码所有图集和独立素材；GitHub Actions 同时执行 Python 逐帧审计。

## 本地静态结果

- 13 张图集、616 格和 110 个独立帧全部完整解码。
- V12/V15/V17/V18 外轮廓绿色溢边：`0.00%`。
- 全部被处理文件的 alpha 通道与 `fdd8355` 原始文件逐像素一致。
- 移动体型漂移不超过 `1.146×`，相邻质心位移不超过 `8.49px`。
- 已登记循环的闭合跳变不超过内部中位帧差的 `1.09×`。
- V18 重建连续执行两次后，30 个母表／运行 PNG 的 SHA-256 完全一致。

Windows Release 编译、测试、发布和安装器生成以 PR 对应的 GitHub Actions 结果为准；Linux 静态结果不冒充 Windows 实机启动验收。
