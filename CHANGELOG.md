# 更新记录

## 1.11.1

- 恢复此前上传时被截断的九张 V18 全分辨率母表与九张运行图，Windows 门禁现在强制解码全部 PNG 像素，损坏图不再进入安装器。
- V12/V15/V17/V18 运行素材统一执行三像素外轮廓去绿边；保持 alpha、轮廓、尺寸、落点和建模形象完全不变。
- 冻干与激光追逐按每方向 `0-1-2-3-2-1` 闭环播放；日常舔毛使用往返循环，避免末帧跳回首帧。
- 动作素材库的单次预览播放到末帧后停住并提供“重新播放”；八方向追逐只预览单一方向的闭合步态，不再把八个视角串成一段跳变动画。
- 素材审计新增完整 PNG/IEND、细微绿色溢边和循环闭合跳变门禁，并在 Windows CI 中运行同一套逐帧审计。
- 恢复主人页、档案、相册、提示词、模型供应商和四类动作页签。
- 主人明确发起的四种魔法使用 OwnerForced 与 ForceInterrupt；笼中、旅行和石化仍作为硬状态门禁。
- 修复冻干／激光投放的双重仲裁，选点不再占租约；激光改用独立 LaserPlay 参与判断和低伏扑光点收尾。
- V18 统一重制趴卧、打滚、追尾、哈欠、逗猫棒、睡姿、舔毛、吃脚、猫砂、牵引绳、蓝窝和斜向步态。
- 冻干与激光拆为两张八方向、每方向四相的独立追逐条带；运行目录移除被替换的旧 PNG，源码历史仍可追溯。
- Windows PR 现在自动执行 Release 编译、测试、素材门禁和 1.11.1 安装器打包。

## 1.11.0

- 银币图集升级为 V17：依据主人参考图重制正视亮银边正面，移除 V16 黑色透视侧缘；五态键、猫爪背面和翻面手势保持兼容。
- 新增 `Pupu.Application` 纯 `net8.0` 应用层与 `Pupu.Platform.Windows` 平台适配层；测试不再直接链接 Desktop 源文件，Windows 凭据和环境探测不再进入 ViewModel。
- `MainViewModel` 的 WPF 图像、裁帧、计时器、动作预览和退出行为改由 `IDesktopPresentationHost` 注入；行为内核组合由 `PetBehaviorRuntime` 独占，禁止 UI 创建第二个仲裁器或提案历史。
- 新增跨平台架构门禁，强制 `Pupu.Behavior` / `Pupu.Application` 不依赖 WPF、Win32 或 Windows TFM，并检查 ViewModel 不绕过表现/平台端口。
- 新增真正的一键安装程序 `Pupu-Setup-x64-1.11.0.exe`：当前用户无管理员安装、桌面／开始菜单快捷方式、系统卸载入口、安装后自动启动和覆盖升级。
- 安装载荷内置 SHA-256 文件清单，解压前限制路径、替换前逐文件校验，并使用暂存目录与旧版本回滚，避免半安装状态。
- 安装器和应用均为 Windows x64 自包含发布，不要求另装 .NET Desktop Runtime；普通卸载保留 `%LOCALAPPDATA%\PupuDesktop` 中的共同记忆和设置。
- 新增平台无关 `PetAgentKernel`，并把输入拆为 `IAgentDecisionStatePort` 与 `IAgentMemoryPort`：前者只返回隔离的评分快照，后者只返回情节、关系事实和习惯摘要；Agent 不再持有可变的持久化性格对象。
- 新增 `BehaviorPresentationIntent` 与可替换表现适配器；当前 sprite 图集继续使用既有 2D 切帧，未来可新增二维骨骼、三维骨骼或程序化表现而不修改 Agent 内核。
- `BehaviorArbitrator` 成为唯一行为决策入口，统一硬资格、性格/状态/关系/记忆评分、迟滞选择、当前行为租约、优先级、保护期、可打断性和冷却；内部状态访问加锁，避免计时器、输入和提案执行并发破坏租约。
- 自主行为和触摸反应使用“评分—选择—提交”同一次仲裁结果；`BehaviorDecision.Admission` 直接交给执行层，不再用另一组参数二次放行。
- `BehaviorProposalExecutor` 完整传递 observation/allowed-state 契约；平台表现缺失、执行失败或取消时回滚本次租约和冷却，不会把宠物卡在幽灵忙碌状态。
- `EligibilityFilter` 与 `BehaviorSelector` 必须注入现有 `BehaviorArbitrator`，不能再各自创建隐藏的规则历史。
- 自动化测试增加到 68 项，新增单次提交、失败回滚、决策快照隔离、表现适配器替换和模型无关记忆端口回归测试。
- 保持 1.10.1 的五态银币状态键、素材清单 schema、记忆 JSON/JSONL/Markdown 格式和现有动作行为兼容。

## 1.10.1

- 银币正式图集升级为 V16，正面采用主人确认的圆眼正脸母版，并清除圆外背景、水印和残留像素。
- `normalColor`、`normalFaded`、`unhappyColor`、`unhappyFaded` 与猫爪 `back` 保持原五态运行时键、圆心、尺寸和翻面逻辑。
- “素材库 → 技术与存储”新增“银币组合更新”卡片，直接从正式清单展示五态实图、状态键、图集坐标和时长。
- 应用、文件、产品和 Windows 发布包版本更新为 1.10.1；其余行为与 1.10.0 保持兼容。

## 1.10.0

- 默认回复与模型提示词改为猫系傲娇风格，拒绝“一本正经报告动作”的机械话术。
- V15 统一重制重点触摸、生活、睡眠、观察、舔毛、如厕、追逐、笼中及完整身体视线素材；透明轮廓无绿色残边。
- 图集切分使用主体连通域、固定中线、统一脚底线与 20px 安全边距，避免截断和逐帧上下乱跳。
- 移动窗口只在步态换帧时推进；激光点和冻干追逐均为八方向、每方向四个不同脚步相位。
- 银币正面圆外透明，主人原图猫脸保持圆眼；背面为同圆心猫爪并支持点击翻面。
- 鼠标视线改成四种高频姿态的完整身体替换帧，并增加方向采样与切换迟滞。
- 性格、长期记忆、动作规则归入“功能设置”；用户动作预览和技术素材映射归入一级“素材库”。
- 全部自主候选暂不可用时保留当前动作并延迟重试，消除 `No behavior candidates passed` 诊断风暴。
- 启动图集最低网格统一为共享契约；`motion` 为 8×10，并有回归断言防止旧 8×11 硬编码复发。

## 1.7.1

- 银币正面改为主人确认的侧脸魔法银币母版，保留 `CAT COIN`、`MAGIC CURRENCY`、月亮、星星和环形魔法纹路。
- 彩色／非彩色开心版共用完全相同的构图；非彩色版仅做确定性去色。
- 不开心版保持直立耳朵与侧脸不变，只让双眼整体轻微下垂并缩小纵向开合，不使用夸张眼角。
- 猫爪背面同步为同银质、同文字、同魔法纹路、同圆心和直径；五态素材继续使用既有 `coinStates` 运行时状态键。

## 1.7.0

- V13 重做四组魔法：紫色斗篷统一增加金色星星，偏小主体恢复固定尺度，石化改为从脚部向上的硬质晶面和石缝。
- 银币补齐 `normalColor`、`normalFaded`、`unhappyColor`、`unhappyFaded`、`back` 五种正式状态，翻面保持同一圆心和直径。
- 新增 16 帧头部局部视线覆盖层、16 方向食物／玩具锚点追逐和 16 方向孔雀蓝背带遛猫。
- 重做如厕四阶段、激光笔两组、需要距离、信任亲近、过度 rua、梳毛、主动求关注和叼绳求遛；窗口上沿趴卧／巡视行为链及素材行已删除。
- 动作组面板增加触发条件。运行目录仅保留清单实际引用的 13 张图集和 3 个独立动作条带。
- V13 重建脚本使用临时文件和原子替换；静态验证覆盖 616 个图集单元格和 48 个独立动作帧。

## 阶段 4（源码，未发布）

- 素材清单增量升级到 schema 2，同时保留 schema 1 `SpriteAtlas + row`。新增 action groups、独立动作文件、帧数／时长／循环、intro／loop／exit、方向、姿态、局部视线、食物／玩具支持和 fallback；旧 1.6.0 素材包继续兼容。
- 动画播放在原 `AnimationSequence` 前增加动作组解析；新来源缺失、字段不全或校验失败时回到动作组 fallback 或原硬编码序列。面板增加动作组元数据、校验结果和逐帧只读预览。
- 新增平台无关 `PersonaDefinition`、`RulePetAgent`、`BehaviorProposalQueue` 与 `BehaviorProposalExecutor`。默认朴朴 Persona 兼容旧档案；规则模式无 API 可运行，LLM 只能增强回复。
- 相册经历行为建议、安静／自主玩耍口令和实际食物／玩具锚点统一经过提案队列、`BehaviorArbitrator` 和平台执行映射。接受、拒绝、延迟、过期、取消和执行失败都有结构化状态；模型输出不能直接创建可执行行为或写长期记忆。
- 素材校验脚本增加 schema 1／2、动作组、五类银币状态、主体尺寸和透明边距契约；清单明确当前除石化外魔法主体偏小，以及正式四种银币正面素材尚待生成。
- 面板改名并新增设计说明、技术实现、模型、相册记忆、行为、素材包、调试、记忆和平台设置区域；调试增加当前意图、Persona、提案队列、提示词边界、Token 粗估、降级原因和素材动作组状态。
- Core 长期边界明确为不依赖 UI 框架、Windows API、LLM 或具体宠物形象；当前仍以 Windows WPF 为唯一实现平台。本阶段未生成 exe、安装包、源码压缩包、`bin`、`obj` 或发布目录。

## 阶段 3（源码，未发布）

- 保留 `albums.json`、原图、逐图描述和阶段 1 缩略图缓存链，新增独立 schema v1 `album-experiences.json`，把图片、Markdown／JSON 发帖和可选旅行返回故事索引为 `AlbumExperienceRecord`。
- Markdown 支持 frontmatter 与无 frontmatter 回退；JSON 支持对应经历字段。所有来源和图片引用限制在相册根目录内，失联、无权限和越界引用只记状态并跳过。
- 经历扫描、指纹比较、检索均在后台执行；新扫描会取消旧扫描，并以 generation 防止过期结果覆盖新结果。面板可保存授权设置、手动重建、查看构建耗时／文件／错误和最近命中预览。
- 规则模式和模型模式共用本地相关性排序。规则模式使用日期、标签、情绪和摘要生成宠物化回忆；模型最多接收 3 条脱敏摘要、有限原文片段和 2 张授权图片。
- 相册经历行为在阶段 3 只提交 `MemoryRecall` 轻建议；阶段 4 已接入本地 PetAgent、提案队列、仲裁和轻动作执行。旅行返回仍先写轻量时间线，开关允许同时追加 `travelEvent` 经历，不进入复杂长期记忆。
- 自动测试登记由 50 项增至 55 项；本阶段未生成 exe、安装包或源码压缩包。

## 阶段 2（源码，未发布）

- 新增 `BehaviorArbitrator`，统一判断行为来源、优先级、最短保护期、可打断性、请求冷却和笼子／旅游等禁用状态；接受与拒绝原因同时进入面板和本地行为日志。
- 普通鼠标注意力改为姿态兼容的方向记录；新增食物和玩具一次性桌面锚点闭环，先经状态参与判断和仲裁，再移动并使用现有动作作为素材回退。
- 本地规则模式新增安静、自主玩耍邀请、食物锚点、玩具锚点、关笼子／释放、旅游／召回口令；模型不参与状态执行。
- 新增持久化关笼子状态与最长 24 小时旅游轻量状态，到期或召回后生成规则模板经历并写入低重要度本地事件。
- 新增仲裁、口令、锚点、笼子、旅游和银币手势测试；本阶段未生成 exe、安装包或源码压缩包。

## 1.6.0

- 正式素材升级为 13 张图集、624 格。V12 重制 Core、Life、Directions、Touch、Routines、WalkModes、LifeEquipment 和 Motion，并新增 `gazeCoin` 3×8 与 `litter` 4×8；V8 Activity、V11 Specials、V10 Seasonal 继续保留。
- 统一重做低趴观察、舔脚吃脚、日常舔毛、侧躺打滚、追尾转圈、冻干／罐头饿猫扑食、检查猫砂、如厕、概率抬头、爪爪开花埋屎、梳毛、过度 rua、信任亲近、偷偷捣乱及自由外出多方向帧，保持拿破仑矮脚、圆脸、长躯干和完整大尾巴。
- 被 V12 取代的旧 V6/V9/V11 正式 PNG 及未使用的 V10 Specials 已从 `Pupu.Desktop/Assets` 移除，保存到 `AssetSources/legacy-formal-v12`；运行包只读取清单登记文件。
- 鼠标视线跟随改用 `gazeCoin:0–1` 两相贴地摆尾；方向不变时也按节拍切换尾巴相位。高信任亲近接入真实触摸路径，并在走近过程中将宠物形象平滑放大至约 106%，结束后恢复。
- 如厕拆成检查进入、低头微动、概率抬头、爪爪开花埋屎和离开四行；如厕后强制衔接埋砂。冻干与罐头投喂拆为单次扑食起步和后半段进食循环，避免反复扑碗。
- 增加 `DesktopRoutePlanner`：遛猫和自主溜达持续生成非零距离、大小混合的随机曲线路段，逐帧更新透明窗口坐标；路线不持久化且不允许原地踏步。
- 扫帚飞行改为每轮打乱八方向桶，覆盖上下左右与四个斜向；使用约 1450–2300 DIP/s 的短时高速大跨度弧线，并在工作区边缘自动回到可继续飞行的位置。
- 主人发起 `Accio Broom`、`Apparate`、`Petrificus Totalus`、`Scourgify` 时直接执行，不再调用普通互动参与概率；自发魔法仍进入统一评分并受每日一次额度限制。
- 新增可点击翻转的双面石化银币：正面为宠物头像、背面为猫爪，以横向压缩和专用过渡帧表现翻转；点击硬币不再误记为普通触摸。
- 透明宠物窗下方增加对话输入框，与面板共享 `ChatInput` / `SendChatCommand`；回复仍统一显示在宠物上方气泡。右键一级“吃饭”改为“吃一下”，“面板”首项改为“打开面板”。
- 动作素材库把普通动作按类别组织为独立页签，魔法和节日仍各自独立；产品设计说明与代码实现说明使用圆角矩阵卡片，并将宠物产品页排在技术配置页之前。
- 相册自动发现根目录下的真实一级子文件夹，从文件夹名解析日期与主题并生成矩阵卡片；支持进入子相册逐张预览、编辑和保存描述，描述参与关键词检索、文字记忆和模型召回。
- 修复 DeepSeek 面板测试：预设使用 `https://api.deepseek.com/chat/completions` 和 `deepseek-v4-flash`，根地址或 `/v1` 地址自动补全 Chat Completions 路径；测试连接不要求先开启对话，也不执行宠物语言边界检查，HTTP 错误仅显示经过清理的服务端摘要。
- `pupu-memory.md` 增加可保存的“宠物系统提示词”段落；保存、重新载入和导出保持该段落，并在固定身份与安全边界内注入高级模型对话的 system 背景。
- Release 验收：Pupu.Tests `43/43` 通过，WPF Desktop Release 编译 `0 warning / 0 error`；绑定检查、C# 语法解析与 XAML/XML 解析通过。624 格素材全部通过，最小有效主体 `67×100px`、最低清晰度 `111.3`、最大移动体型漂移 `1.238×`、最大相邻中心位移 `8.54px`。

## 1.5.0

- 锁定 V11 拿破仑矮脚身份：修正新增生活、装备和斜向移动素材中偏长的腿部比例，保持银灰黑白长毛、圆脸、短口鼻、圆眼、鼻头小黑斑、偏长躯干和完整大尾巴；不覆盖无关 V9/V8 身份素材。
- 新增 `lifeEquipment` 4×8 图集：每日自主如厕与强制埋砂、低频舔胸腹毛、蓝色长方形垫子及窝内睡眠、孔雀蓝牵引绳完整预设动作。
- 新增 `motion` 11×8 图集：背带/无背带四个斜向步态、紫色斗篷扫帚八方向飞行、矮脚侧躺微动和窗口上沿趴卧。
- `specials` 升级为 V11 5×8：保留已验收低趴视线，四种魔法统一紫色斗篷与流光；扩大 Apparate 弧光、补充魔法装扮重现、银币高光和 Scourgify 放大擦拭光环。
- 如厕改为每天随机 2–3 个本地时隙；同日计划幂等，错过时隙不离线追补。抬头为低概率变体，如厕后固定抓挠埋砂；主人界面不再显示手动铲屎入口。
- `self.groom` 改为原地低频日常舔毛，最短 75 秒、冷却至少 10 分钟；新增 `rest.bed`，疲劳且安全时在蓝色长方垫内至少驻留 7 分钟，冷却至少 15 分钟。
- 地面路线按真实位移向量选择八方向素材并保持步态相位；窗口行为继续使用真实 HWND、窗口矩形、DPI 和监视器工作区，目标失效时自动回到普通待机。
- 扫帚飞行改为八方向高速曲线和轻微上下浮动；Apparate 重现后保留约 1.5 秒魔法装扮；解除石化必须先伸懒腰；Scourgify 沿窗口四边或屏幕工作区边缘移动，不修改真实桌面图标。
- 新增宠物相册页：链接真实本地图片根目录，按相对目录、主题、时间和成长阶段建立子相册索引；删除索引不删除原图，目录失联时仍保留元数据。
- 宠物档案页增加专属画像、基于长期性格和跨天偏好的自动摘要，以及可编辑好感阶段展示；展示覆盖不修改底层关系数值。
- 高级调试改为圆角矩阵卡片；动作素材库保留“魔法特辑”，节日限定严格使用独立纯文字“节日特辑”，不提供缩略图或预览。
- 模型 API 增加 OpenAI、Qwen、DeepSeek 和 Custom 预设，支持 Chat Completions / Responses；API 密钥按服务商与地址隔离存入 Windows 凭据管理器。
- 新增 8–12 轮短期会话持久化，并与长期记忆共同注入 System Prompt。相册图片只有在视觉和相册发送两个开关同时开启时才发送，每轮最多 2 张，且不发送本地绝对路径。
- 正式清单扩展为 11 张图集、576 格；自动测试 `38/38` 通过。素材最小有效主体 `69×100px`、最低清晰度 `111.3`、最大移动体型漂移 `1.236×`、最大相邻中心位移 `9.01px`。

## 1.4.0

- 新增宠物档案页，可编辑并保存中文名、英文名、品种、性别、宠物生日、对主人昵称、与主人关系和主人生日；档案同步进入本地确认事实、宠物自称、主人称呼及模型角色提示。
- 新增近距离全局鼠标视线跟随：宠物原地且没有面板前台操作或特定互动时，以八方向 V10 素材即时转动眼睛和头部；不改变窗口位置。
- 主人从右键发起的吃饭、遛猫、清理、梳毛、玩具、姿势和魔法均经过当前饱腹、精力、疲劳、压力、关系、性格与具体偏好参与概率；拒绝使用角色台词说明且不扣信任。
- 新增一级“宠物魔法”及 `Accio Broom`、`Apparate`、`Petrificus Totalus`、`Scourgify` 四项英文二级入口；分别实现一分钟扫帚飞行、当前屏随机移形、银币石化待解除和窗口/屏幕边缘擦亮。
- 宠物每天最多自发随机施法一次；额度在动作开始前持久化。同日手动施法不占用自发额度。
- 新增圣诞帽、万圣节斗篷帽、春节红围巾和主人生日祝福；仅在精确日期触发，生日有年份时结合主人年龄。
- 动作素材库增加“魔法特辑”页签与日期可用性标签；正式素材扩展为九张图集、456 格。
- 自动测试从 30 项扩展为 34 项，新增状态参与差异、每日魔法额度、节日日期门槛和档案语言身份测试。

## 1.3.0

- 核心、触摸、方向与行走模式图集升级到 V9；按最新高清实拍统一宽圆幼态脸、短口鼻、常态圆眼和鼻斑，重新绘制趴卧呼吸、逗猫棒、困倦哈欠、触摸边界、信任亲近及整套孔雀蓝背带。
- 触摸边界改为回避视线、甩尾、转身和离开，不使用炸毛、哈气、露齿或攻击姿态；哈欠只保留在困倦行。
- `GestureInterpreter` 的短期互动负担改为 6 秒滑动窗口；第一次触摸增加硬门槛，连续 rua 才能逐级进入需要空间或离开。
- 普通侧躺和低趴显式设置 120 秒最短驻留；默认策略升级到 120 秒，自然语言调整范围收敛为 90–300 秒。
- 行走图使用闭合半步往返序列，方向切换保留归一化步态相位，避免换向时从第 0 帧重启。
- 宠物文字气泡改为内容自适应宽高，短文本不再保留固定大块空白。
- 素材审计新增移动行表观体型漂移与相邻帧主体中心位移门槛；384 格验收结果为有效主体至少 `69×101px`、最低清晰度 `111.3`、最大体型漂移 `1.214×`、最大中心位移 `9.01px`。
- 新增触摸首击边界与停手清空负担测试，自动测试从 28 项扩展为 30 项；旧 V6 核心/触摸/方向/行走与 V7 Activity 从正式资源目录归档。

## 1.2.0

- 新增 `PetSpeechComposer`：宠物气泡按五维性格、当前压力和互动意图生成角色台词；API、错误、连接、评分和路径等技术信息只在系统面板显示，模型回复越界会被拦截。
- 新增 OpenAI 风格模型 API 配置与对话；非敏感设置保存在 `model-api.json`，API 密钥保存在 Windows 凭据管理器。删除原“打开 ChatGPT/复制粘贴网页回复”入口与桥接服务。
- 双击不再打开面板，统一作为同一 `InteractionSession` 中的两次触摸；面板只从右键菜单进入，连续快速触摸仍由手势解释器识别。
- 新增 Windows 窗口上沿感知，只使用窗口几何、进程类别、前台状态和显示器工作区；浏览器优先，支持趴卧、沿边行走和窗口移动跟随，不读取窗口内容。
- 桌面移动、窗口上沿、拖放、边界收敛和控制面板定位改为当前监视器工作区，修复扩展屏互动后跳回主屏的问题。
- Activity 图集升级为 `pupu-activity-youthful-v8.png`，增加圆脸板鸭趴和窗口上沿趴卧；最新三张实拍作为身份参考，旧 V7 不再发布。
- 素材审计扩展为 384 格有效像素、20px 透明边距、完整 PNG 解码、最小主体尺寸和清晰度检查。
- 自动测试从 25 项扩展为 28 项，补充双击会话语义、宠物台词技术隔离及窗口信号硬前置测试。

## 1.1.0

- 增加激光笔右键互动和两套习性化反应；增加三种持续睡姿与睡姿过渡，昼夜信号进入自主决策。
- 将行为决策硬拆为 EligibilityFilter、UtilityScoring、SelectionPolicy 与 ActionScheduler，安全限制不再依赖扣分。
- 增加 RuntimeState 有界恢复、休眠恢复，InteractionSession、逐帧互动区域、感知事件去抖/合并/习惯化。
- 增加 ConfirmedProfileFact、RawInteractionEvent、EpisodicMemory、DerivedHabitPreference 四层记忆及删除级联。
- 新增传感器轨迹回放等测试，自动测试从 13 项扩展为 25 项。
- 控制面板调整为科技简约与温馨并存的轻卡片布局；主人互动只保留在桌面右键菜单。
- 素材包增加 `pupu-activity-youthful-v7.png`，共七张图集、368 格。

## 1.0.0

- 实现 PersonalityBehaviorV2，把 TemperamentBaseline、RuntimeState、RelationshipState 和 LearnedPreference 分开持久化。
- 所有自主候选统一使用可配置 BehaviorDefinition 和 BehaviorScorer；加入可注入时钟/随机源、75 秒自主驻留下限、冷却、重复抑制、迟滞、打断成本与完整评分日志。
- 五维天生性格进入日常自主行为分布；淘气不再依赖长期忽略，独立不再延长生气跑开或降低信任。
- 新增 GestureInterpreter 两阶段触摸；去除点击次数直接绑定动画与敏感 75% 梳毛硬阈值。
- 投喂、铲砂、梳毛、逗猫棒和散步按实际进度提交效果，记录 Started/Progressed/Completed/Interrupted/Failed；自由探索也可安全停止。
- 删除自动性格偏移回流；习惯至少跨 3 天形成，主人纠正精确作用于 behavior_id、interaction_type、context 和动画来源。
- 增加版本化幂等迁移与 LegacyLearningSnapshot；离线和多日无互动不再扣状态、累积欠账或触发责怪行为。
- 新增无第三方测试框架的 Pupu.Tests，覆盖 13 项分布、迁移、生命周期和 24 小时稳定性验收。

## 0.9.0

- 右键分级菜单新增一级“停下”，不再藏在“玩一下”二级菜单；背带遛猫和其他可取消长动作可直接叫停，无背带自由外出仍不接受点击停止。
- 合并 0.7.0 与 0.8.0 的全部 40 个动作/表情预览入口；安静陪伴增加上一版的蜷尾陪伴与侧背转换，同时保留新版 75–160 秒长驻留机制。
- 以 0.7 的幼态圆脸、柔和毛感为形象基准，以 0.8 的完整全身、无手和 256px 安全区为技术基准，重建六套 320 帧 v6 图集。
- 统一素材清晰度为中等：五官、眼睛和爪子清楚，毛发使用柔软毛束而非单根毛超锐细节；修正尖脸、错位眼睛、扭曲口鼻及跨动作忽锐忽糊。
- 素材切分改为先识别透明主体连通区域，再按动作行和帧中心归组；即使原始猫身或尾巴跨过理论格线，也不会切到相邻帧。
- 新增行级/整图双重 PNG 解码、落盘与原子替换，避免文件尾写入不完整；所有正式图集仍保持 20px 安全区、完整全身、无硬黑边和触摸组无手。
- 异步命令、随机桌面移动、UI 调度和未观察任务异常统一写入 `pupu-error.log`；普通可恢复异常不再让应用直接退出。
- 停止后立即恢复可操作状态，并用动作令牌防止旧动作的延迟收尾覆盖用户刚启动的新动作。
- 检测到 0.8.0 自动导出的旧素材副本时优先使用 0.9.0 内置 v6 图集，避免本地缓存继续覆盖新版形象。

## 0.8.0

- 全面重建六套 320 帧高清素材：每格提升为 256×256、四边至少 20px 透明区；逐格检查完整头部、躯干、四肢和整条尾巴，去除旧黑块、串格、网格残线及切身问题。
- 触摸图集 48 格全部改为只有 pupu 本体，不出现手、手臂、手指、皮肤或袖口；撸猫动作以猫对“不可见触摸”的反应表现。
- 归一化脚本改为整行动作共用缩放率、统一底部锚点，避免逐帧拉满造成体型和落脚点跳变；补充 320 格非空、安全区和 PNG 完整解码审计。
- 鼠标左右键互换：左键轻点／连点负责 rua 与情绪升级，左键按住仍可拖动；右键只打开分级菜单。
- 右键一级菜单改为“吃饭”“玩一下”“铲屎”“梳毛”“面板”，移除宠物旁常驻入口；食物、遛猫、玩具、缩放、ChatGPT 等收进二级菜单。
- “陪伴”改成无按钮的自主状态，同一安静动作组至少驻留约 75 秒，再在侧躺、低趴、舔脚和背影之间切换。
- 升级素材清单与动画引擎到 256 单格；旧 192 本地素材自动回退内置新版，不会阻断启动。

## 0.7.0

- 新增两套共 128 帧素材：侧躺、低趴、舔脚、三类进食、完整背影/侧影，以及孔雀蓝背带和无背带四方向移动；总素材达到 320 帧。
- 移除带“操作”文字的常驻按钮，改为无文字 `⋯` 入口；右键猫本体直接进入 rua/烦躁/生气互动。
- 新增孔雀蓝背带遛猫与解除背带自由外出：每次运行时重新随机生成目标、曲线路径、速度、小跳和停顿；背带状态可右键停止，自由状态不响应点击停止。
- 投喂拆分为猫粮、冻干和罐头；猫粮磨蹭慢吃，冻干和罐头使用急切扑食节奏。
- 默认安静行为改为侧躺、低趴、舔脚和少量背影回头，不再以端坐作为常驻姿态。
- 素材清单扩展为六套图集，仍可从 `%LOCALAPPDATA%\PupuDesktop\assets` 独立替换并自动校验回退。

## 0.6.0

- 根据新增实拍参考重做 192 帧动作图集：幼态圆脸、黄绿色眼睛、粉黑拼接鼻头中央带黑色、三头身但较长躯干、矮脚和超大尾巴。
- 风格调整为柔和半写实毛绒感，去掉上一版偏沧桑的成年脸，同时避免 Q 版大头短身。
- 睡眠、翻滚和陪伴帧继续强制完整身体；四向行走按长躯干幼猫比例重新制作。
- 新增外部 `pupu-assets.json` 与 `AssetPackService`：图集不再嵌死在程序集，主人可从控制面板导出到本地素材目录后直接替换。
- 自定义素材会校验网格与尺寸；配置错误时自动回退内置素材，不阻断应用启动。

## 0.5.1

- 为 `CurrentBehaviorLabel` 增加 WPF 兼容写入边界：即使旧 BAML 或控件模板意外请求 TwoWay，也不会再因只读属性中止窗口创建；当前 XAML 仍明确使用 `OneWay`。
- 每次进入应用启动阶段都会向 `pupu-error.log` 写入带版本号和 Windows 版本的 `START` 标记，便于区分旧日志、旧 EXE 与当前构建。
- 启动气泡会明确显示 `pupu 0.5.1 已启动`，避免从旧解压目录误运行历史版本。

## 0.5.0

- 新增直接点击宠物的触摸系统：单击/轻点会慢眨眼、蹭脸、歪头询问或呼噜踩奶；短时间连续点击会经历压耳甩尾警告、炸毛哈气、挥爪抱踹和桌面跑开。
- 触摸阈值与反应概率由信任、五维性格、长期动作权重和主人纠正共同计算；“隐藏动作”页实时展示当前烦躁/生气阈值、呼噜概率和跑开时间。
- 新增 48 帧触摸情绪图集，总动作素材增至 192 帧；控制面板新增“动作素材库”，可浏览全部 25 个动作组的真实缩略图、说明、帧数并逐项预览。
- 宠物旁操作项改为默认隐藏的弹出菜单，仅保留一个小“操作”入口；弹出菜单不会改变透明宠物窗口布局。
- 区分点击与拖拽：短按松开触发 rua，超过系统拖拽阈值才移动窗口。停止按钮现在也会中止触摸反应、素材预览和生气跑开。
- 新增主人可编辑的 `pupu-memory.md` 和可读时间线 `events.md`；旧 `events.jsonl` 可自动迁移，控制面板支持浏览、修改、保存和重新载入 Markdown 记忆。
- 新增性格—记忆—动作匹配说明；触摸、纠正和长期互动会有限地更新动作偏好和性格演进，同时保留主人设定的性格底色。
- 自然语言规则新增触摸阈值、跑开时长和轻触反应偏好，例如“疯狂点击8次会生气跑开10秒”“轻轻摸会呼噜或眨眼”。
- 新增“Codex 迭代”页和仓库 `AGENTS.md` / `PUPU-CODEX.md` / `ACTION-LIBRARY.md`：可把新动作或设定连同当前本地上下文生成 Markdown 任务并打开 Codex；不读取 Cookie、不冒用登录、不要求 API key。

## 0.4.0

- 新增 32 帧四方向移动图集，桌面遛猫会根据路径实时切换左、右、上、下朝向，不再始终朝左。
- 遛猫改为真正的持续状态：默认 6 分钟并持续生成路径；投喂、铲砂、梳毛、逗猫棒和长睡分别使用可配置的秒/分钟时长，所有长动作均可用“停下”提前终止。
- 新增投喂、清理、梳毛和逗猫棒的慢速往返循环，减少首尾跳帧；四方向 32 格全部重做安全边距，消除猫身裁切及邻格残片。
- 修复气泡隐藏时使用 `Collapsed` 导致透明窗口重新排版的问题；改为固定高度并以 `Hidden` 保留布局，原地趴卧、躺卧、吃饭和睡觉不再产生窗口位移。
- 新增长时间原地睡觉与打呼噜：低精力必然触发，深夜和长期被忽略时按概率触发，默认持续 30 分钟。
- 控制面板新增“隐藏动作”页，逐条显示 rua 抗议、饥饿、猫砂、遛猫提醒、求关注、捣乱、睡眠和普通待机的触发阈值、概率及移动边界。
- 自然语言设定扩展到动作时长和播放速度，例如“每次遛猫10分钟”“投喂持续1分钟”“睡觉30分钟”“动作慢一点”。

## 0.3.0

- 新增 64 帧生活互动图集，为投喂、铲屎、摸摸/过度rua、梳毛、求关注、捣乱、安静陪伴和求遛猫分别提供连续动作；连同原有安全图集共 112 帧。
- 每种动作使用独立帧时长：呼吸与休息明显放慢，吃饭、梳毛与互动采用中速，跑动与转圈采用快速节奏。
- 自主动作增加最短停留时间、提醒冷却和长时趴卧，减少频繁切换和弹气泡。
- 新增本地自然语言角色管理：可直接描述被忽略后的行为、每日遛猫、捣乱阈值、动作切换间隔、性格增减，以及添加/删除长期记忆。
- 自然语言规则写入 `behavior-rules.json`，手动记忆写入 `profile.json`，同时进入 ChatGPT 网页会话卡。
- 重新规范两套图集为 192×192 安全格，统一缩放与中心锚点并清理跨格残片，修复尾巴、躺姿被裁切和切帧错位。
- 新增短距离自主走动：没人理时可在当前位置附近慢慢来回寻找主人；正式遛猫仍使用全桌面随机路线。

## 0.2.0

- 重构为透明、贴桌面的散养宠物窗口；动画不再塞在固定大面板中。
- 照顾按钮以文字标签紧挨在 pupu 旁边，遛猫时自动隐藏，结束后恢复。
- 新增 48 帧动作表情图集：趴卧呼吸、侧躺打滚、转圈、红色逗猫棒、眨眼/哈欠/舔爪/小脾气，以及多姿势跑动。
- 支持按钮和鼠标滚轮缩放，范围 55%–180%，大小写入本地状态。
- 遛猫改为桌面随机路径，含随机转向、停顿、跳跃弧线、跑步、转圈和扑跳姿势。
- 移除 OpenAI API key 配置，改为系统浏览器 ChatGPT 会话桥：复制本地会话卡、打开 chatgpt.com，并可把网页回复粘贴回本地记忆。

## 0.1.2

- 修复行为说明文字 `CurrentBehaviorLabel` 因 `Run.Text` 默认双向绑定而触发的第二个启动错误。
- 完整审计界面绑定：所有只读展示属性显式使用 `OneWay`，所有可编辑属性显式使用 `TwoWay`，避免控件默认值再次造成同类问题。

## 0.1.1

- 修复六个生活状态进度条错误使用双向绑定，导致应用首次启动时崩溃的问题。
- 启动阶段如果再遇到可恢复的数据绑定错误，应用会记录具体原因并继续运行，不再只显示通用提示。

## 0.1.0

- 首个 Windows 原生桌面宠物版本。
