# Pupu Desktop 1.3.0 产品功能说明

## 1. 产品定位

Pupu Desktop 是运行在 Windows 10/11 桌面上的透明窗口宠物。它以真实朴朴为角色原型：银灰黑白长毛、圆脸、宽白鼻梁、黄绿色眼睛、粉黑鼻头、矮脚和蓬松大尾巴。

产品不把宠物做成等待命令的工具。朴朴有稳定的天生性格、会变化的短期状态、与主人的关系和逐步形成的具体偏好；它可以主动休息、玩耍、靠近、巡视或拒绝互动。主人通过右键菜单发起照料和玩具互动，面板用于设置、记忆、诊断与对话。

## 2. 1.3.0 目标

本版在 1.2.0 的窗口感知、多显示器和角色对话基础上，重点解决六类体验问题：

1. 行走腿部节奏不闭合，转向后从首帧重启；
2. 安静动作驻留过短，原地陪伴不够稳定；
3. 同一动作或方向切换时表观体型变化；
4. 第一次触摸也可能进入强烈边界反应，无法体现“偶尔喜欢 rua、持续撸会离开”；
5. 文字气泡固定尺寸，短句留下大块空白；
6. 早期趴卧、逗猫棒、触摸关系和孔雀蓝背带素材的脸型与清晰度落后于最新形象。

## 3. 角色与发言

### 3.1 发言通道

界面把信息分为两个通道：

- 宠物气泡：只显示朴朴说的话；
- 系统状态：只在面板或系统对话框显示连接、API、错误、路径、版本、日志、评分等技术信息。

异常不得把堆栈、文件路径或模型错误放入宠物气泡。可恢复异常发生时，朴朴只会说符合性格的简短安抚句，详细原因留在对应状态区。

### 3.2 性格一致性

宠物台词至少读取：

- 活泼；
- 黏人；
- 敏感；
- 独立；
- 淘气；
- 当前压力；
- 当前互动意图。

高黏人的朴朴更愿意表达陪伴，高独立的朴朴会保留距离，高活泼会主动邀请玩耍，高敏感在边界被越过时更直接，高淘气会有轻微的小脾气。所有版本都保持自主性，不承诺处处听从主人，也不会以“主人没有回应”为理由责怪、降信任或升级提醒。

### 3.3 技术语言隔离

宠物气泡禁止出现 API、模型、提示词、系统消息、评分、行为 ID、状态数值、代码、日志、调试、版本、文件路径、ChatGPT、Codex 等实现语言。

内置台词和模型回复都经过同一边界检查。模型回复越界、为空或过长时不会显示，应用改用本地性格台词。

## 4. 桌面互动

### 4.1 鼠标语义

- 单击：一次轻触；
- 双击：同一连续互动会话中的两次轻触，不打开面板；
- 短时间高频点击：由手势解释器识别为 `rapid_tap`；第一次触摸有硬门槛保护，只有持续越过边界才可能触发回避或跑开；
- 按住：按住/抱起意图；
- 按住后明显移动：拖动桌面宠物窗口；
- 滚轮：缩放宠物；
- 右键：打开主人互动菜单；
- 面板：只能由右键菜单打开。

连续触摸在同一 `InteractionSession` 中记录，避免双击被拆成两个独立学习样本。短期触摸负担使用 6 秒滑动窗口：停手后自然清空，不把较早的偶尔 rua 永久累计成烦躁。

### 4.2 主人发起的互动

右键菜单提供：

- 吃饭：猫粮、冻干、罐头；
- 玩一下：逗猫棒、激光笔、散步方式；
- 铲屎；
- 梳毛；
- 停下；
- 面板；
- 缩放。

激光笔会根据朴朴当前疲劳、压力、活泼度、玩耍意愿和具体偏好，在“趴低扭屁股追”“原地伸爪扑”或保持观察之间选择。主人发起不等于宠物必须接受。

### 4.3 宠物主动诉求

朴朴可低频发出求关注、求玩或求遛提示。提示频率和形式受到性格、状态、冷却、重复抑制、勿扰和全屏限制。主人无回应时会话自然结束，不扣信任，不升级催促。

## 5. 窗口与显示器环境

### 5.1 窗口感知

应用只读取 Windows 提供的：

- 可见窗口边界；
- 窗口是否最小化；
- 前台状态；
- 进程类别；
- 显示器与工作区几何；
- DPI。

应用不读取窗口正文、网页 DOM、标签页、网址、标题内容、屏幕像素、键盘输入或用户情绪。普通键鼠活动不能推断会议、情绪或网页内容。

### 5.2 窗口上沿行为

当同一显示器上存在足够宽、未最小化、位置安全的普通应用窗口时，朴朴可以：

- 移动到窗口上沿；
- 前爪搭边趴卧；
- 沿上沿左右巡视；
- 在窗口移动或尺寸变化后跟随更新位置。

浏览器窗口和当前前台窗口有选择优先级，但窗口上沿行为必须先通过真实环境硬前置条件；不能只靠评分强行触发。全屏、安静时段和安全限制仍由 EligibilityFilter 直接禁止。

### 5.3 多显示器

拖动、自动移动、窗口栖息、边界收敛和控制面板定位都使用宠物当前监视器或目标窗口所在监视器的工作区。宠物在扩展屏互动后不会因为引用主屏工作区而跳回主屏。

显示器配置变化、休眠唤醒和系统时间变化会刷新环境状态。首次发布后仍建议在真实混合 DPI 多显示器设备上做一次冒烟测试。

## 6. 自主行为与动作连续性

行为选择依次经过：

1. EligibilityFilter：硬前置、安全、全屏、勿扰、环境信号；
2. UtilityScoring：性格、状态、关系、上下文、偏好、重复与冷却；
3. SelectionPolicy：对高分候选使用可注入的可复现随机策略；
4. ActionScheduler：进入、循环、退出和安全中断。

自主行为具有最短驻留、切换迟滞、冷却和重复抑制。睡眠使用 3–6 分钟级最短驻留，普通侧躺和低趴默认至少 120 秒；自然语言即使要求加快，也不低于 90 秒。动画以进入帧、往返循环、退出或转换帧连接，避免突然跳姿势。

四向行走与背带行走使用闭合半步序列 `0→1→2→3→4→3→2→1`。方向改变时把旧序列的归一化相位映射到新序列，不从第 0 帧重新起步。移动图集还必须通过表观体型和主体中心位移门槛。

白天提高持续睡眠倾向；夜间在疲劳、压力、勿扰和安全条件允许时提高玩耍与巡视倾向。

## 7. 1.3.0 动作与素材

正式素材包含七张 256×256 单元格透明图集，共 384 格：

- V9 核心动作：重做趴卧呼吸、逗猫棒和困倦哈欠；
- V6 生活互动；
- V9 四向移动；
- V9 触摸关系：重做轻触、呼噜、好奇、需要空间、转身离开和信任亲近；
- V6 日常待机与进食；
- V9 孔雀蓝背带/自由四向移动；
- V8 激光、睡眠、板鸭趴和窗口上沿。

V8 Activity 图集为 8×8：

| 行 | 动作 |
|---:|---|
| 0 | 激光笔·趴低扭屁股追 |
| 1 | 激光笔·原地伸爪 |
| 2 | 团成一团睡 |
| 3 | 四仰八叉睡 |
| 4 | 侧身伸展睡 |
| 5 | 睡姿平滑转换 |
| 6 | 板鸭趴（两条后腿向后分开） |
| 7 | 窗口上沿趴卧 |

最新三张实拍图用于校准圆脸、眼距、额头 M 纹、白鼻梁、鼻斑和动态仰头姿势。前两张高清图决定身份和比例，第三张低清图只提供姿态，不决定纹理细节。眼睛平时保持圆润，困倦时才半闭；张大嘴只用于困倦哈欠。边界素材以移开视线、甩尾、转身和离开为主，不出现炸毛、哈气、露齿或攻击动作。

每个正式单元格必须满足：

- 256×256 RGBA；
- 完整身体与尾巴；
- 四边至少 20px 透明区；
- 无相邻格残片；
- 有效主体短边至少 64px、长边至少 96px；
- 清晰度不低于项目审计阈值；
- 移动行表观包围盒面积漂移不超过 1.25×；
- 相邻移动帧主体中心位移不超过 10px；
- 图集可完整重复解码。

低于阈值的素材不进入发布包；旧 V6 核心、触摸、方向、行走和旧 V7 Activity 已归档，不随 1.3.0 发布。

## 8. 模型 API 对话

### 8.1 配置

“和朴朴聊天”页面支持兼容 OpenAI Chat Completions 或 Responses 请求格式的服务：

- 可启用/关闭；
- 配置 HTTPS endpoint；
- 配置模型名；
- 配置温度；
- 保存或删除 API 密钥；
- 测试连接；
- 发送对话。

远程地址必须使用 HTTPS，本机服务可使用 `localhost`。未配置或服务失败时，朴朴仍使用本地性格台词。

### 8.2 隐私和安全

- API 密钥保存在 Windows 凭据管理器；
- `model-api.json` 只保存非敏感设置；
- API 密钥不进入日志、记忆和聊天记录；
- 系统提示锁定角色、自主性、简短表达和技术语言禁区；
- 返回文本再次经过本地边界校验；
- 已删除旧的“打开 ChatGPT、复制会话卡、手动粘贴回复”功能。

## 9. 面板

面板使用低饱和青灰色作为科技强调，暖橙色表示陪伴与提示。首屏只展示需要理解的状态与入口，详细评分、日志和模型连接信息放入独立页面。桌面宠物文字气泡使用 112–320px 自适应宽度和随内容增长的高度；短句保持紧凑，较长文本自动换行。

面板承担：

- 状态与关系概览；
- 天生性格设置；
- 自然语言规则；
- 隐藏动作与评分诊断；
- 动作素材预览；
- Markdown 记忆；
- 模型 API 对话；
- Codex 迭代入口。

面板不重复摆放投喂、玩具、铲砂等主人互动按钮。

## 10. 数据与记忆

记忆分为：

- ConfirmedProfileFact；
- RawInteractionEvent；
- EpisodicMemory；
- DerivedHabitPreference。

决策只读取索引后的派生偏好，不遍历全部原始记录。习惯形成考虑跨日、样本数、发生机会、结果、上下文一致性和矛盾证据，并设影响上限。删除证据后重新计算派生习惯，被删除证据不会在下次整理时恢复。

长时间离线不逐分钟模拟，不产生照料欠账或责怪。休眠与重启只应用有界恢复。

## 11. 验收

1.3.0 交付前必须满足：

- Pupu.Behavior、Pupu.Tests 和 WPF Desktop Release 编译通过；
- 自动测试 30/30 通过；
- 第一次触摸不能选择 warning/avoid/run_away；停止 6 秒后旧触摸负担清空；
- 双击测试证明不会打开面板且属于一个连续会话；
- 台词测试证明技术词被拦截且不同性格给出不同表达；
- 窗口上沿行为没有操作系统表面信号时不可用；
- 384 格素材全部通过尺寸、透明边距、清晰度、移动体型漂移、帧间中心位移与解码检查；
- Windows x64 自包含包包含 V9/V8 正式图集和两份 1.3.0 说明；
- 应用包与源码包可以完整解压；
- 发布素材与源码素材哈希一致。

## 12. 已知边界

- 当前未提供代码签名、自动更新和 Microsoft Store 包；
- 当前窗口识别基于 Win32 几何和常见浏览器进程名，不理解网页内容；
- Linux 构建环境无法执行真实 WPF 桌面 UI 自动化；多显示器、混合 DPI、休眠唤醒和窗口快速跨屏需要 Windows 实机冒烟；
- 自定义模型服务的兼容程度取决于其 Chat Completions 或 Responses 响应结构。
